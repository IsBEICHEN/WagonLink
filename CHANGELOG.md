# 更新记录

本文件按版本号归并记录，同一版本内的界面、播放、订阅和修复项合并展示。

## 4.1.0

- 更新应用图标版本到 `4.1.0`，并重新生成 debug 安装包。

## 4.0.1

- 将刷新订阅功能版本更新为 `4.0.1`，并重新生成 debug 安装包。

## 4.0

- 更新应用版本号到 `4.0`，并提供对应 debug 安装包。

## 3.8.2

- 更新应用版本号到 `3.8.2`，并提供对应 debug 安装包。

## 3.8

- 设置页新增“返回时自动进入悬浮窗”开关。
- 开启悬浮窗后，播放中按返回键离开主播放页或切到后台时自动进入小窗播放。
- 悬浮窗支持拖动位置，并通过右下角“调整”控件改变窗口大小。
- 悬浮窗位置和尺寸会自动保存，下次自动进入小窗时沿用上次设置。
- 首次开启悬浮窗会跳转系统权限页，请允许 WagonLink 显示在其他应用上层。
- 悬浮窗默认隐藏频道信息，仅保留右上角关闭入口；点击小窗主体可返回应用全屏播放。
- 修复后台进入悬浮窗时可能因播放器 Surface 切换导致 ANR 的问题。
- 优化播放器图标，进入全屏、退出全屏、锁定、解锁和悬浮窗关闭按钮改为简洁白色线框样式。
- 移除播放器普通页和全屏页信息浮层、锁定按钮、全屏按钮、悬浮窗关闭按钮的胶囊背景。
- 普通播放页全屏入口调整到视频右下角，和全屏退出入口保持一致。

## 3.7

- 修复新版启动闪退问题，恢复被错误编码破坏的界面字符串。
- 修复订阅页进入时可能添加空视图导致崩溃的问题。
- 订阅列表支持点按直接加载，长按弹出加载、编辑、删除操作。
- 编辑订阅时按订阅 ID 更新，避免修改链接后被保存成重复订阅。
- 支持解析并缓存 M3U 频道里的 `http-user-agent`、`http-referrer` / `referer` 请求头。
- 播放时按频道请求头创建 ExoPlayer 数据源，提高部分 APTV 类订阅源兼容性。
- 更新频道缓存结构，缓存频道请求头并自动忽略旧结构缓存。
- 修复订阅列表显示错误，保存的订阅现在显示真实订阅名称。
- 更新版本号到 `3.7`，新增 `WagonLink-3.7-debug.apk` 发布包。

## 3.6

- 重新规划主界面结构，底部拆分为频道、订阅、设置三个标签页。
- 将外观和版本信息移动到设置页，订阅页只保留订阅源管理。
- 添加左上角 `+` 新增订阅入口。
- 全屏播放默认隐藏所有浮层控件，点击屏幕后再显示操作。
- 增加全屏锁定和解锁按钮，锁定后只保留解锁入口，减少误触。
- 移除频道列表左侧两个字符的圆形标记，列表显示更简洁。
- 调整底部菜单圆角、页面过渡动画和整体毛玻璃风格。
- 优化全屏控件位置，减少与播放器控制条、状态栏和系统信息重叠。

## 3.5

- 修复跟随系统日夜模式切换时直播中断的问题，播放中不再因 `uiMode` 变化重建页面。
- 增加全屏播放保活，播放和全屏时保持屏幕常亮。
- 优化全屏下点击屏幕显示控件的逻辑。
- 修复底部按钮部分遮挡和全屏退出按钮位置冲突。
- 将版本号保持为 `3.5` 的相关 UI 修复合并记录。

## 3.2

- 增加 APK 下载说明，并在仓库 `releases` 目录提供 debug 安装包。
- 完善 README 中的安装、构建和使用说明。
- 说明直播源受 token、地区、源站限制或鉴权影响时可能无法播放。

## 3.1

- 更换应用图标和应用名称为 WagonLink。
- 支持保存多个订阅源，并在频道页选择切换。
- 支持打开应用时自动加载上次选择的订阅。
- 增加订阅解析缓存，避免频繁进入频道页或订阅页时重复解析。
- 增加频道加载中状态、播放失败提示和当前直播源高亮。

## 3.0

- 调整为毛玻璃风格界面，并移除普通界面分支。
- 订阅页面显示当前软件版本。
- 优化频道页、订阅页和底部菜单布局。
- 增加跟随系统、日间模式、夜间模式的外观选择。
